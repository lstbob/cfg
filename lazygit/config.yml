# Default lazygit configuration. Theme-specific overrides removed.
# lazygit ships sensible built-in defaults; this file is a placeholder so the
# symlink target still exists until a real preference is added here.